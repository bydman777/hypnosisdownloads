name: authentication_logic
description: Package that provides authentication logic.

publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  error_handling:
    path: ../error_handling
  equatable: ^2.0.5
  firebase_auth: ^5.3.2
  firebase_core: ^3.7.0
  flutter:
    sdk: flutter
  json_serializable: ^6.4.0
  json_annotation: ^4.7.0
  flutter_bloc: ^9.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  freezed_annotation: ^2.2.0
  dio: ^5.4.0
  shared_preferences: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.2.1
  hive_generator: ^2.0.1
  freezed: ^2.2.0
